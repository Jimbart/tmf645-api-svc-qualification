/**
 * Enum for standard uses.
 * Date format, encoding, etc.
 * @readonly
 * @enum {string}
 */
const STANDARD = Object.freeze({
  ENCODING: 'utf-8',
  TO_BASE64: 'base64',
  DATE_FORMAT: 'YYYY-MM-DD HH:mm:ss',
});

export default STANDARD;
