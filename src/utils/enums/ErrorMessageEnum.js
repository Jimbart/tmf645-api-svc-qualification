/**
 * Enum for error messages.
 * @readonly
 * @enum {string}
 */
const ERROR_MESSAGE = Object.freeze({
  NO_SERVICE_QUAL_FOUND: 'No service qualification found',
  EXPIRED_RECORD: 'Record already expired',
  LPDS_ID_NOT_FOUND: 'Lpdsid not found in AMS',
});

export default ERROR_MESSAGE;
